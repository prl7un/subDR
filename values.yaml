dr_active: false

image:
  # kind load docker-image 로 dr-cluster 노드에 미리 적재된 로컬 이미지를 쓴다(레지스트리 푸시 불필요).
  repository: subdr-app
  tag: test
  pullPolicy: IfNotPresent

service:
  port: 80
  targetPort: 8000
